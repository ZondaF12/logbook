import { View, Text, SafeAreaView } from "react-native";
import React from "react";

const index = () => {
    return (
        <SafeAreaView>
            <Text>index</Text>
        </SafeAreaView>
    );
};

export default index;
