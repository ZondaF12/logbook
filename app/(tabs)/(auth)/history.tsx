import { View, Text, SafeAreaView } from "react-native";
import React from "react";

const History = () => {
    return (
        <SafeAreaView>
            <Text>History</Text>
        </SafeAreaView>
    );
};

export default History;
