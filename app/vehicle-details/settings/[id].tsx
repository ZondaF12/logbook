import { View, Text } from "react-native";
import React from "react";

const VehicleSettings = () => {
    return (
        <View>
            <Text>VehicleSettings</Text>
        </View>
    );
};

export default VehicleSettings;
