export interface VehicleRequest {
    registration: string;
    color: string;
    engine_size: number;
    make: string;
    model: string;
    tax_date: string;
    mot_date: string;
    registered: string;
    year: number;
}
